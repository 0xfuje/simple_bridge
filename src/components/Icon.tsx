import React from 'react';
import styled from 'styled-components';

const StyledIcon = styled.select`
    
`;

function Icon() {
    return (
        <StyledIcon>
            
        </StyledIcon>
    )
}

export default Icon;
